# Mobile does not use DATABASE_URL — it calls the VNDRLY API.
# Copy values to repo-root `.env.local` (loaded by `pnpm run dev:local`).

EXPO_PUBLIC_DOMAIN=https://vndrly.ai

# Point at local api-server while developing on desktop:
# EXPO_PUBLIC_DOMAIN=http://localhost:8080
